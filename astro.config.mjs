// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
    site: 'https://brennan-creadevlabs.github.io',
    base: '/astro-tutorial',
});
